# Iniciar o projeto

1. Iniciar o xamp

2. rodar npm run dev


# commit alterações site

## Localmente

1. se mexe em front precisa rodar o build antes, ai exclui o que existe atraves de `rd /s /q public\build` e depois pra contruir outro `npm run build`

2. ai dentro do ssh depois de puxar os arq do git rodar `php artisan optimize:clear`


1. logar via ssh

2. cd ~/domains/gestocar.com.br/public_html

3. git branch se for main ok

4. git pull origin main